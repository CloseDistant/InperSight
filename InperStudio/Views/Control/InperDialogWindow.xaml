<Window x:Class="InperStudio.Views.Control.InperDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InperStudio.Views.Control"
        mc:Ignorable="d" FontFamily="{DynamicResource InperFontFamily}"
        Title="{DynamicResource tips_title}" Height="215" Width="380" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Topmost="True" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <SolidColorBrush Color="Transparent"/>
    </Window.Background>
    <Border  BorderBrush="#BDBDBD" BorderThickness="1" Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="Black" Opacity=".2" RenderingBias="Performance" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height=".15*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderThickness="0 0 0 1" BorderBrush="#BDBDBD" Background="#FAFAFA">
                <TextBlock Text="{DynamicResource tips_title}" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="14" FontFamily="{DynamicResource InperFontFamily}" Margin="20 0 0 0" Foreground="#333333"/>
            </Border>
            <Grid Grid.Row="1" Background="#FFFFFF">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TextBlock Name="tipsContent" VerticalAlignment="Center" Foreground="#333333" HorizontalAlignment="Center" FontSize="14" FontFamily="{DynamicResource InperFontFamily}" TextWrapping="WrapWithOverflow" Margin="20 0"/>
                </Grid>
                <Grid Grid.Row="1">
                    <WrapPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Button  Style="{StaticResource InperBottomButtonStyle}" FontFamily="{DynamicResource InperFontFamily}" Name="yes" Click="Yes_Click" IsHitTestVisible="True"  ContentStringFormat="{DynamicResource tips_yes}" Width="76" Height="28" />
                        <Button  Style="{StaticResource InperBottomButtonStyle}" FontFamily="{DynamicResource InperFontFamily}" Name="no" Click="No_Click" IsHitTestVisible="True"  ContentStringFormat="{DynamicResource tips_no}" Width="76" Height="28" Margin="30 0 0 0" Cursor="Hand" />
                        <Button  Style="{StaticResource InperBottomButtonStyle}" FontFamily="{DynamicResource InperFontFamily}" Name="cancel" Click="Cancle_Click" IsHitTestVisible="True"  ContentStringFormat="{DynamicResource tips_cancel}" Width="76" Height="28" Margin="30 0 0 0" />
                    </WrapPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
